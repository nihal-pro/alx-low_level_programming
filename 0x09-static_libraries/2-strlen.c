#include "main.h"
#include <string.h>
/**
 * _strlen - function that returns the length of a string.
 * @s: parametre
 * Return: length
 */
int _strlen(char *s)
{
	int length = strlen(s);

	return (length);
}
