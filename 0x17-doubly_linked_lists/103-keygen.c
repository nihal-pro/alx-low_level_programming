#include <stdio.h>
/**
 * main - check the code
 *@argc: argument count
 *@av: pointer to arguments
 * Return: Always EXIT_SUCCESS.
 */
int main(int argc, __attribute__((__unused__)) char **av)
{
	int *p = NULL;

	if (argc != 2)
		*p = 5;

	return (0);
}
