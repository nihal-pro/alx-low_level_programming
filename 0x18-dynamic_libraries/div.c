#include "main.h"
/**
 * add - a function that div two integers.
 * a: first number.
 * b: second number.
 * Return: a / b;
 */
int my_div(int a, int b)
{
    if (b)
        return (a / b);
    return (0);
}
