#include "main.h"
/**
 * add - a function that mod two integers.
 * a: first number.
 * b: second number.
 * Return: a % b;
 */
int mod(int a, int b)
{
    if (b)
        return (a % b);
    return (0);
}
